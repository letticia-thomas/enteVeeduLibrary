<div class='card'>
  <div class='card-header'>
    {{pageTitle + ': ' + book?.bookName}}
  </div>

  <div class='card-body' *ngIf='book'>
    <div class='row'>
      <div class='col-md-8'>
        <div class='row'>
          <div class='col-md-4'>Name:</div>
          <div class='col-md-8'>{{book.bookName}}</div>
        </div>
        <br>
        <div class='row'>
          <div class='col-md-4'>Language:</div>
          <div class='col-md-8'>{{book.language | uppercase | convertToSpaces: '-'}}</div>
        </div>
        <br>
        <div class='row'>
          <div class='col-md-4'>Description:</div>
          <div class='col-md-8'>{{book.description}}</div>
        </div>
        <br>
        <div class='row'>
          <div class='col-md-4'>Author:</div>
          <div class='col-md-8'>{{book.author}}</div>
        </div>
        <br>
        <div class='row'>
          <div class='col-md-4'>Price:</div>
          <div class='col-md-8'>{{book.price|currency:'INR':'symbol'}}</div>
        </div>
        <br>
        <div class='row'>
          <div class='col-md-4'>5 Star Rating:</div>
          <div class='col-md-8'>
            <pm-star [rating]='book.starRating'>
            </pm-star>
          </div>
        </div>
        <br>

        <div class='row'>
          <div class='col-md-4'>Owner:</div>
          <div class='col-md-8'>{{book.reader}}</div>
        </div>
        <br>
        <div class='row' *ngIf="!isAvailable || editAuth">
          <div class='col-md-4'>Currently reading:</div>
          <div class='col-md-8'>{{book.currentReader}}</div>
        </div>
        <br>

        <div class='row'>
          <div class='col-md-4'>Available On:</div>
          <div class='col-md-8'>{{book.availableOn}}</div>
        </div>
        <br>
        <div class='col-md-4'>
          <img class='center-block img-responsive' [style.width.px]='200' [style.margin.px]='2' [src]='book.imageUrl'
            [title]='book.bookName'>
        </div>
      </div>
    </div>

    <div class='card-footer'>
      <button class='btn btn-outline-secondary' style='width:80px' (click)='onBack()'>
        <i class='fa fa-chevron-left'></i> Back
      </button>
      <button *ngIf="editAuth" class='btn btn-outline-secondary' style='width:80px' (click)="edit()">
        <i class='fa fa-chevron-right'></i> Edit
      </button>

      <button *ngIf="isReturn && !editAuth" class='available' style='width:80px' (click)="returnBook()">Return Book
      </button>

      <button *ngIf="isAvailable && !editAuth" class="available" style='width:80px' (click)="readTheBook()">
        <i>Click here to get this book</i>
      </button>

      <em *ngIf="!isAvailable && !editAuth" class="" style='width:80px'>
        Not available! {{book.currentReader}} reading this book now
      </em>

    </div>
  </div>

  